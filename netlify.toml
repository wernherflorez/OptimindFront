[build]
  command       = "npm run build"
  publish       = "dist"

# SPA routing – envía todo a index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
